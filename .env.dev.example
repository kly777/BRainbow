# ── 开发环境配置模板 ──
# 复制为 .env.dev 使用（make dev 自动加载）
# 前端: vite --mode dev 读取（vite.config.ts 的 loadEnv 指向项目根）
# 后端: cargo run 在 debug 构建下自动加载（main.rs 的 dotenvy）

# ── 前端（vite dev server） ──
VITE_PORT=3001
VITE_API_TARGET=http://localhost:3000
VITE_SITE_URL=http://localhost:3001

# ── 后端（cargo run） ──
SERVICE_PORT=3000
BIND_HOST=127.0.0.1
DATABASE_URL=sqlite:brainbow.db
CORS_ALLOW_ORIGIN=http://localhost:3001

# 是否开放注册（公网部署建议保持 false，需要时手动开）
ALLOW_REGISTER=false

# ── 视频海报帧（可选） ──
# 给视频文件出列表缩略图需要 ffmpeg/ffprobe。取一份静态构建：
#   make fetch-ffmpeg        # 下载 + 校验 sha256 → vendor/ffmpeg/bin/（版本与哈希见 deploy/ffmpeg.lock）
# 然后取消下面这行的注释。没配也不影响其他功能：视频只有后缀徽章 + 时长。
# 相对路径按后端进程的工作目录解析（make dev / make dev-backend 都从仓库根启动）。
# FFMPEG_PATH=./vendor/ffmpeg/bin/ffmpeg
